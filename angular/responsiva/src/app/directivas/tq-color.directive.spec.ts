import { TqColorDirective } from './tq-color.directive';

describe('TqColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TqColorDirective();
    expect(directive).toBeTruthy();
  });
});
